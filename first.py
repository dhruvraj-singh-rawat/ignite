import random # Used to Generate Random Number

random_arr=[] # Creating an Empty Array
for x in range(1000000):  
  random_arr.append(random.randint(1,101)) # Appending Random Number Generate in range of (1,100) to the Array

pr